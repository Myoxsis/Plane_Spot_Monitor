import SpotterTable from '@/components/SpotterTable';export const dynamic='force-dynamic';export default function SpotterPage({searchParams}:{searchParams:{[k:string]:string|string[]|undefined}}){const sp=searchParams||{};const get=(k:string)=> typeof sp[k]==='string'? sp[k] as string : Array.isArray(sp[k])? (sp[k] as string[])[0] : undefined;const initial={airport:(get('airport')||'CDG').toUpperCase(),direction:(get('direction') as 'arrivals'|'departures')||'departures',minScore:parseInt(get('minScore')||'0',10)||0};return(<div className='space-y-4'><h1 className='text-xl font-semibold'>Spotter board</h1><SpotterTable initial={initial} /></div>) }
